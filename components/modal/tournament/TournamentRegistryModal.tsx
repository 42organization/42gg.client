import { TournamentInfo } from 'types/modalTypes';

export default function TournamentRegistryModal({
  tournamentDate,
  tournamentDiscription,
  tournamentName,
}: TournamentInfo) {
  return <div>TournamentRegistryModal;</div>;
}
