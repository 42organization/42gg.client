export interface ProfileInfo {
  userId: string;
  userImageUri: string;
  rank: number;
  ppp: number;
  wins: number;
  loses: number;
  winRate: string;
  racketType: string;
  statusMessage: string;
}
